# TODO — Paczka Organizer

Lista zadań do odhaczania. Claude Code utrzymuje ją samodzielnie (patrz `CLAUDE.md`,
sekcja „TODO.md”): odhacza po ukończeniu (`- [x] … (RRRR-MM-DD)`), dopisuje nowe
pozycje, nieaktualne przekreśla z powodem. Źródło planu: `docs/ARCHITEKTURA_FINALv1.md`
(sekcja 13). Nazwy skryptów zgodne z README (Quickstart).

Legenda: `[ ]` do zrobienia · `[x]` zrobione · `[~]` w toku · ~~przekreślone~~ nieaktualne

## A. Pierwszy przebieg (fundament, robiony raz)

- [x] A1. Szkielet repo + `config/` (paths, subjects, syntax, thresholds) + Claude Code setup (2026-09-17)
- [x] A2. Model danych — sekcja 4: `scripts/orglib/{schema.sql,db.py,config.py,hashes.py}`, `scripts/db_admin.py`, 70 testów (2026-09-17)
- [x] A2a. Naprawa `config/subjects.yaml` (sem 7: klucz `wspolne`, poprawny YAML) (2026-09-17)
- [x] A0. Bootstrap skali dublowania: `rmlint -D` + `ncdu` na źródłach (tylko raport) → `reports/bootstrap/bootstrap_rmlint.txt`: 47 991 plików, 29 548 kopii, 17,94 GB dubli (~45%); pełne artefakty w `20_WORK/` (2026-09-17)
- [x] A3. `scripts/scan.py` — stat źródeł → `source_packages`, `folders`, `files` (status `discovered`), `structural_signature`; skip niezmienionych poddrzew na re-runie; generuje `reports/SOURCES_TREE.md` (2026-09-17)
- [x] A4. `scripts/hash_files.py` — sha256 → `files.sha256`, `content` (content_kind z `orglib/kinds.py`), status `hashed`; wznawialny, `--retry-errors`, containment ścieżek (2026-09-17)
- [x] A5. `scripts/fold_hash.py` — `tree_hash` / `content_set_hash` → `folders.duplicate_of` (dwa przejścia, FK) + `reports/folder_overlap.csv` (próg z `thresholds.yaml`) (2026-09-17)
- [x] A6. `scripts/dedup_report.py` — unique vs duplicate (liczby, bajty, per paczka) → `reports/dedup_summary.md`, `reports/inventory.jsonl` (2026-09-17)
- [x] A7. `scripts/scan_target.py` — skan istniejącej `paczka/` w `target_repo` = ground truth (lock; hash + ścieżka docelowa → `applied`/klasyfikacja `manual`, conf=1.0) (2026-09-17)
- [x] A9. **Decyzja użytkownika:** szablon docelowy dopasowany do ground truth — `{SKROT}_{Nazwa}` (bez nawiasów) na SEM1-4; SEM5/6 dodatkowy poziom strumienia (`Aplikacje`/`Systemy`/`Wspolne`); SEM7 dodatkowy poziom katedry, w repo katalog to DOKŁADNIE `{KOD}_{profil}` (np. `KAIMS_Algorytmy_I_Modelowanie_Systemów`, nie sama `KAIMS`) albo `Wspolne` bez profilu — `Subject.grupa` = `f"{katedra}_{profil}"`. Skróty przemianowane zgodnie z ground truth (stare jako aliasy): sem1 HDI→HDMI, sem3 AK→AKO, sem3/4/5 JAI→JAII/JAIII/JAIV (sem2 JAI bez zmian), sem6 PGI→PGII, sem6 ZAK→ZAKO, sem7 PDII→PDIII, sem7 SDII (dawniej powtórzone w 6 profilach)→jeden SDIII pod `wspolne`, sem7 KAIMS "JPNP."→JPNP (i usunięta kropka przed NET w nazwie). Dokładna pisownia/wielkość liter dopasowana 1:1 do folderu (probe je wymusza): sem1 HIH→HiH, sem2 PEIM→PEiM, sem2 AISD→AiSD, sem4 SWIM→SWiM, sem4 MPWI→MPwI (stare skróty jako aliasy), sem5/6 `nazwa` PGI/PGII → `PROJEKT_GRUPOWY_I/II` (całe wielkimi literami, jak w repo), profile SEM7 przemianowane na dokładne nazwy folderów (`Algorytmy_I_Modelowanie_Systemów`, `Architektura_Systemów_Komputerowych`, `Bazy_Danych`, `Inteligentne_Systemy_Interaktywne`, `Systemy_Geoinformatyczne`, `Teleinformatyka`). `config.TARGET_PATH_TEMPLATE`/`TARGET_PATH_TEMPLATE_GROUPED` + `Subject.grupa`/`.katedra` w `scripts/orglib/config.py`. **Probe** `tests/test_subjects_vs_target_repo.py` (pomijany bez lokalnego `target_repo`): 98 przedmiotów w obie strony — 0 brakujących target_dir (poza jawną listą wyjątków `{(2,"WFI"),(3,"WFI")}` — brak materiałów), 0 katalogów w repo bez wpisu w `subjects.yaml`. (2026-09-17)
- [x] A10. Po A9 (renames AKO/ZAKO/TRP/HDMI/JAII-IV/PGII/PDIII/SDIII + katedry SEM7) ponowny `scan_target.py` na realnej `target_repo`: **sklasyfikowane 1647 / bez dopasowania 30** (z 1677 unikalnych treści; `bez dopasowania` liczy RAZEM oba poniższe kubełki). ZAKO, TRP, HDMI i jego podprzedmioty (Prawo_Patentowe/Twórczość_Inżynierska/Wiedza_O_Kulturze — trafiają teraz pod HDMI jako `category`) już się dopasowują — z realnych braków (dopisanie aliasu by nie pomogło) zostały:
  - **foldery bez dopasowania (18 treści, prawdziwa luka w strukturze, nie w subjects.yaml):** `SEM{1..6}`: pliki luzem wprost pod semestrem, bez podfolderu przedmiotu (`tutorial_sem_N_*.pdf`, `ranking_nauczycieli_akademickich.pdf`, 17 plików) + `SEM7/KAIMS_Algorytmy_I_Modelowanie_Systemów/CODE_CLEANER_GOLUCH.zip` (plik wprost w katedrze, bez podfolderu przedmiotu).
  - **poza zakresem klasyfikacji (12 treści, strukturalnie nigdy nie będą przedmiotem):** `ogolne/` (skrypty, tutoriale, test.txt), `SEM{1,2,5}/sources/`, `Magisterskie_SEM2/Inżynieria_systemów_informacyjnych/ZBI_…` (magisterskie, por. D3). (2026-09-17)
- [x] A9 runda 3. Użytkownik przemianował ręcznie 3 katalogi w `target_repo` (branch
  `fix/nazwy-katalogow-przedmiotow`, **czeka na merge do mastera przez użytkownika** —
  ten branch to jedyne źródło ground truth dla tej rundy): `PGI_PROJEKT_GRUPOWY_I` →
  `PGI_Projekt_Grupowy_I` (SEM5), `PGII_PROJEKT_GRUPOWY_II` → `PGII_Projekt_Grupowy_II`
  (SEM6), `SI._Serwisy_Internetowe_.NET` → `SI_Serwisy_Internetowe_NET` (SEM7/KASK).
  Dopasowano `subjects.yaml` (`nazwa` PGI/PGII na `Projekt_Grupowy_I/II`; KASK SI
  skrót `"SI."` → `SI`, `nazwa` → `Serwisy_Internetowe_NET`, stary skrót jako
  alias `"SI."`) — co odsłoniło NOWĄ kolizję: SEM7 ma teraz SI dwa razy (KASK
  `Serwisy_Internetowe_NET` vs KT `Sieci_IP`), więc samo (semestr, skrot) w SEM7
  przestało być jednoznaczne. Rozwiązanie: `config.find_subject(..., *, grupa=None)`
  — z `grupa` zawęża kandydatów do `Subject.grupa == grupa` (katedra/profil w
  SEM7, strumień w SEM5/6); `scripts/scan_target.py: classify_path` przekazuje
  jako `grupa` OSTATNI pominięty segment ścieżki (katalog katedry/strumienia) i
  przy `KeyError`/`ValueError` z grupą wraca do wywołania bez niej (nigdy
  odwrotnie — zły traf w grupę nie ma prawa podmienić trafnego dopasowania na
  zgadywankę). `iter_subjects()` też przeliczony: kontrola duplikatów wewnątrz
  YAML teraz kluczuje po (semestr, skrot, grupa), nie samym (semestr, skrot).
  Testy: `tests/test_subjects_vs_target_repo.py` nadal 0/0 (czyta branch z
  renamami). Pełny `scan_target.py` na realnej `target_repo`: **sklasyfikowane
  1647 / bez dopasowania 30** (bez zmiany — te same liczby co A10, bo to
  przemianowania istniejących katalogów, nie nowa treść), **usunięte
  nieaktualne wpisy ground truth: 12** (stare ścieżki sprzed rename). (2026-09-17)
- [x] ~~A8. `config/taxonomy.yaml`~~ — zbędny: `syntax.yaml` (kategorie, foldery, formy, media) JEST taksonomią; architektura §3/§13 poprawiona (2026-09-17)

## B. Skrypty cyklu per-przedmiot

- [x] B1. `scripts/prepare_subject.py --semester N --skrot X [--grupa G]` + `orglib/subject_manifest.py` — deterministyczny wycinek indeksu SQLite read-only, jeden rekord/SHA-256, pełne provenance, reprezentant spoza `duplicate_of`, niejasności do review; bez skanu materiałów i bez zmian klasyfikacji. `just subject-prepare` działa. Domyślnie `reports/{SKROT}/manifest_slice.jsonl`; powtarzające się skróty izolowane w `reports/SEM{semester}/{grupa}/{SKROT}/`. Atomowy zapis i ochrona drzew materiałów; 59 testów jednostkowych/CLI na syntetycznym indeksie. (2026-09-17)
- [x] B2. `scripts/extract_text.py` + `scripts/orglib/textextract.py` — głowa tekstu (PDF przez PyMuPDF z awaryjnym pdfplumber, DOCX, PPTX, tekst/kod/CSV z fallbackiem cp1250) do `20_WORK/extracted_text/{sha256}.txt`; `normalized_text_hash`, własny deterministyczny `simhash` (blake2b, shingle 3 słów, hex 64 bit) i `perceptual_hash` (imagehash) w `files`; `content.extracted_text_path`/`ocr_done`; status `extracted`. Recepta `just extract`. Ustalenia:
  - **praca raz na treść**: druga kopia tego samego sha256 i ponowny przebieg biorą gotowy tekst z dysku (smoke: 1,6 s → 0,2 s), `--force` wymusza ekstrakcję od nowa; poddrzewa `duplicate_of` odsiewa `db.files_pending`;
  - **OCR jest awaryjny, nie domyślny**: uruchamia się dla PDF dopiero, gdy warstwa tekstowa i pdfplumber dały < `ocr_min_chars` (120) znaków, i wchodzi do wyniku tylko wtedy, gdy dał WIĘCEJ treści niż warstwa tekstowa (krótki, poprawny PDF nie ma prawa zostać zastąpiony szumem). Obrazy wymagają jawnego `--ocr-images`, `--no-ocr` wyłącza całość;
  - **awaria pdfplumber jest tolerowana** (to tylko druga opinia o układzie strony), **awaria OCR leci dalej jako błąd** — nierozpoznany skan ma trafić na status `error` i czekać na `--retry-errors`, nie udawać pustego;
  - **brak tekstu ≠ błąd**: archiwum, media, `.doc`/`.rtf`/`.odt`, `.xlsx` (brak openpyxl w zależnościach) i obraz bez OCR przechodzą na `extracted` z metodą `unsupported` i `extracted_text_path = NULL`;
  - `--text-dir` wskazujący `sources`/`target_repo`/`media` kończy się kodem 2; ścieżka w bazie zapisywana względem `work` (przenośna), containment źródeł wspólny przez nowe `config.resolve_within_sources()` (`hash_files.py` przestawiony na nie zamiast własnej kopii);
  - 96 testów (`tests/test_textextract.py`, `tests/test_extract_text.py` — zestaw napisał Codex, przejrzany i uzupełniony przez koordynatora; atrapa OCR z zakazem niezamówionego wywołania tesseractu, realny `paths.yaml` przekierowany do `tmp_path`, zero sieci). (2026-09-18)
- [x] B2a. `text_head` w manifeście B1: `prepare_subject.py` czyta głowy tekstu z `content.extracted_text_path` (tylko z `work`, wpis wskazujący drzewo materiałów jest pomijany) i dokłada **opcjonalne** pole `text_head` obcięte do `thresholds.yaml: llm.max_text_head_bytes`. Domyka wiszący kontrakt: `ai_resolve.py` czytał `row["text_head"]`, którego nikt nie produkował, więc klasyfikacja szła po samej nazwie pliku. (2026-09-18)
- [x] B2b. Domknięcie formatów w etapie extract (decyzja użytkownika: dokładamy zależności zamiast zostawiać treści bez tekstu). Zmierzone na realnym indeksie: 345 unikalnych treści było nieczytanych z powodu braku bibliotek. Dodane: `openpyxl` (.xlsx/.xlsm — 107 treści), `xlrd` (.xls — 5), `odfpy` (.odt 27 / .ods 15 / .odp), a stare formaty binarne `.doc` (124), `.ppt` (34), `.pps` (33) idą przez **systemowy** `catdoc`/`catppt` (wybór użytkownika: catdoc zamiast LibreOffice — ~1 MB zamiast ~1 GB, goły tekst w zupełności wystarcza do klasyfikacji). Brak konwertera daje metodę `no_converter` widoczną w podsumowaniu — **nie** błąd i **nie** ciche `unsupported`; doinstalowanie pakietu + `just extract --force` domyka temat bez zmian w kodzie. Mapa `orglib/kinds.py` uzupełniona o `.jfif` (53 treści, to JPEG), `.pps`/`.ppsx` (55), `.xlsm`, `.hs` (41). Ponieważ `content_kind` ustala etap hash, dołożona komenda `db_admin.py refresh-kinds` (dry-run domyślnie, zapis z `--apply`, reguła „pierwsze rozszerzenie wygrywa” jak w `hash_files.py`) — przeliczyła 149 treści w realnym indeksie. `setup/requirements.txt` i `setup/install.sh --with-apt` zaktualizowane (pakiet `catdoc` w liście apt, w kontroli braków i w nagłówku użycia). Użytkownik zainstalował `catdoc` 2026-09-18; **weryfikacja na realnych plikach ze źródeł** (read-only) pokazała krzaki w `.doc` — `catdoc` zakłada cp1252, więc polskie znaki się psuły („Zminimalizowaæ funkcjê”). Stąd jawne kodowanie źródłowe `DEFAULT_LEGACY_CHARSET = cp1250` i opcja `--legacy-charset`; dla `.ppt`/`.pps` flaga jest obojętna (UTF-16), ale podajemy ją jednolicie. Przy okazji naprawiony test zależny od środowiska: sprawdzał „brak tekstu” na `.doc`, więc przechodził tylko dopóki `catdoc` nie był zainstalowany — teraz używa `.rtf`, a ścieżkę konwertera pokrywają testy z atrapą `subprocess`. Testy: 577 w całym zestawie. (2026-09-18)
- [x] B2c. **Decyzja użytkownika:** testy mają być czerwone, gdy środowisko nie działa — nie wolno przerabiać testu, żeby przeszedł. Powstała osobna warstwa `tests/test_environment.py` (marker `environment`, recepta `just env-check`, 38 kontroli) BEZ atrap: realny zapis i odczyt PDF/DOCX/PPTX/XLSX/ODT, phash obrazu, OCR wyrenderowanej strony przez tesseract (+ kontrola języka `pol`), odczyt `.doc` przez `catdoc` z weryfikacją polskich znaków, obecność binarek (`catdoc`, `catppt`, `tesseract`), komplet pakietów z `requirements.txt` i minimalne wersje (`pymupdf >= 1.24` — od niej istnieje nazwa modułu `pymupdf`; `xlrd >= 2.0` — 2.x czyta wyłącznie `.xls`). Sprawdzone empirycznie, że warstwa działa: przy ukrytym `catdoc` czerwienieje 5 testów po nazwie, a po symulacji regresji kodowania — `test_catdoc_decodes_polish_source_charset`. Ta druga próba obnażyła kruchość kodu: `DEFAULT_LEGACY_CHARSET` jako domyślna wartość argumentu wiązał się przy imporcie, więc podmiana stałej nie miała skutku; `extract()` czyta ją teraz w momencie wywołania (`legacy_charset: str | None = None`). Testy: 615 w całym zestawie. (2026-09-18)
- [x] B2d. **Audyt testów (decyzja użytkownika: „jeśli coś nie działa, ma być czerwone”).** Czterech agentów-audytorów z wymogiem dowodu przez mutację przejrzało 7,5 tys. linii testów. Wynik: kod produkcyjny w większości poprawny, ale trzy klasy kontraktów niepilnowane przez nic. Naprawione w tej kolejności:
  1. **Guard źródeł — 10 realnych obejść** (zweryfikowanych osobno przez podanie hookowi ładunków na stdin): `git -C … clean/reset`, ścieżka schowana w zmiennej, `os.chdir` + relatywny `remove`, `ruby File.write`, cel kopiowania nie na końcu, `zip`/`7z`/`tar -c` do źródeł, `python3 -m zipfile`, a **najgroźniej** — dowolna nazwa narzędzia inna niż `Bash` (dyspozytor i oba matchery zakładały tę nazwę, więc dla powłoki Codeksa ochrona nie uruchamiała się w ogóle). Guard reaguje teraz na treść zdarzenia i czyta komendę podaną też jako lista argumentów. Listy słów w testach są **wypisane wprost** — parametryzacja po liście z implementacji była pusta (skrócenie listy w kodzie skracało zestaw przypadków przy 32 zielonych testach). Doszedł test, że hook jest w ogóle podpięty w configach obu hostów.
  2. **Symlinki w bramce zapisu `ai_resolve`** — jedyna realna dziura w kodzie poza guardem: bliźniacza kontrola w `prepare_subject` rozwijała dowiązania, ta nie. Wspólny `config.check_output_target`.
  3. **Cztery skróty w mojej własnej pracy B2**: reguła `refresh-kinds` niezgodna z `hash_files` (utajona — na realnym indeksie 47 treści z wieloma rozszerzeniami, 0 rozjazdów), zamrożona lista formatów w teście środowiska, `catppt` akceptujący atrapę `exit 0`, limity stron sprawdzane tylko jako argumenty.
  4. **Strażnicy kontraktów międzymodułowych**: wektory złote hashy wyprowadzone ręcznie z formatu bajtowego (zamiana separatorów przechodziła całe repo!), `kinds.py` ↔ `CHECK` w `schema.sql`, realny `paths.yaml` i `thresholds.yaml`, placeholdery promptu ↔ kod, walidacja backendu przy wczytaniu configu zamiast przy pierwszym płatnym wywołaniu, sonda ground truth przestaje połykać błędy configu. Plus 13 strażników w warstwie scan/hash/db napisanych przez Codeksa (konto OpenAI) i zweryfikowanych niezależnie.
  5. **Dwie nowe warstwy testów**: `cli_contract` (argv kontra parser prawdziwych CLI, zero promptów i zero kosztu — wyłapuje historyczną wpadkę `--ignore-user-config` przed `exec`) i `e2e` (syntetyczna paczka przez cały łańcuch; pilnuje m.in. że `text_head` dociera z etapu extract do manifestu). `pytest.ini` z `--strict-markers`, recepty `just test-fast|env-check|cli-check|e2e|probe`, opis pięciu warstw w README wraz z wpadką, po której każda powstała.
  Testy: **723** (było 615). Każda poprawka zweryfikowana mutacyjnie — regresja czerwieni konkretny test po nazwie. (2026-09-18)
- [ ] B3. `scripts/classify.py` — deterministyczny + heurystyka (regex lab/kol/egzamin/rok/prowadzący); semestr rozstrzyga skrót; `forms` waliduje; status `classified` albo `unresolved`
- [x] B4. `scripts/orglib/llm_client.py` — backend anthropic / openai / `claude -p` / `codex exec` / `agy -p` (Antigravity = Gemini) z `thresholds.yaml: llm` (per zadanie `classify`/`relate`); CLI read-only + schemat JSON; cache decyzji po sha256. `LLMClient.complete()` + `LLMConfig`/`TaskConfig`/`LLMResult`, wyjątki `LLMError`/`LLMTimeout`/`LLMParseError`, `extract_json()` (odporny na płoty markdown/prozę), `truncate_head()`; cienki CLI `scripts/llm_client.py` (typer, wzorzec jak inne skrypty — nie argparse); schemat `prompts/plan_line.schema.json`; cache SQLite `20_WORK/ai_cache.sqlite`; 30 testów w `tests/test_llm_client.py` (mockowany `runner`, zero sieci). Smoke test na kontach: `claude -p`, `codex exec -o`, `agy -p --sandbox` — wszystkie trzy działają (2026-09-17)
- [x] B5. `prompts/classify_ambiguous.md`, `prompts/relate_cluster.md` + `scripts/ai_resolve.py` (poza sesją, masowo). Skrypt bierze `manifest_slice.jsonl` (B1), wybiera pozycje `needs_review` (albo wszystkie z `--all`), buduje prompt z szablonu, woła backend z `thresholds.yaml: llm` przez `LLMClient` i dopisuje `plan.ai.jsonl` zgodny z `prompts/plan_line.schema.json`. Wznawialny (pomija sha256 już zapisane), `--dry-run`, `--limit`, `--no-cache`; progi `confidence` z `thresholds.yaml` są wiążące i nadpisują deklarację modelu; kategoria musi wynikać z form przedmiotu (`allowed_categories()` łączy `subjects.yaml: forms` z `syntax.yaml: categories.*.forms`). `wire_schema()` tłumaczy kanoniczny schemat na wariant przyjmowany przez tryb strukturalny OpenAI (wszystkie klucze w `required`, bez `pattern`/`minimum`) — bez tego `codex exec --output-schema` odbija żądanie z `invalid_json_schema`. Recepta `just subject-ai-resolve SEM SKROT`. Zweryfikowane end-to-end na `codex_cli`/`gpt-6-astra`: wykład → `copy` (conf. 0.99), nieczytelny skan → `quarantine` + `needs_review`. (2026-09-18)
- [ ] B6. `scripts/near_dupe.py` — simhash/MinHash/phash → `relations` (near_duplicate / older_version / related); nigdy nie kasuje
- [ ] B7. `scripts/build_plan.py` → `reports/{SKROT}/plan.jsonl` (schema_version, `_meta`, plan_hash)
- [ ] B8. `scripts/validate_plan.py` — schemat, `..`, kolizje targetów, `syntax.yaml` lint, bramka confidence, dry-run diff; exit≠0 blokuje apply
- [ ] B9. Review: `scripts/review_report.py` — diff HTML near-dupe (`difflib.HtmlDiff`), miniatury, lista `unresolved`, `STATUS.md`
- [ ] B10. `scripts/apply.py` — kopiowanie wg planu na branch `subject/{SKROT}` w `target_repo` (snapshot przed), status `applied`
- [ ] B11. `scripts/verify.py` — hash po kopii == sha256, drzewo == plan, status `verified`
- [ ] B12. `scripts/provenance.py` — `reports/provenance.jsonl` + README per przedmiot do `paczka_meta/` + `00_SOURCES/linki.txt` z `source_packages`
- [ ] B13. `scripts/media.py` — pliki > progu → `90_MEDIA/{skrot}/…` + wpis w `inne/nagrania.txt`
- [ ] B14. `scripts/manual_decisions.py` — CLI do zapisu decyzji z review (conf=1.0) + eksport do `reports/manual_decisions.jsonl` (eksport/import już w `db_admin.py`)

## C. Narzędzia i integracje

- [~] C1. `justfile`: wspólne launchery agentów, first-pass i stabilne nazwy etapów subject gotowe; `subject-start`/`subject-pr` oraz pełne spięcie plan/apply czekają na B1–B14
- [ ] C2. Generator issues z `subjects.yaml` (`gh issue create`, labels semester/subject/status)
- [ ] C3. `synapse` — graf relacji (notatki `.md` z wikilinkami); **po pilotażu**, na realnych danych
- [ ] C4. Pełny diff-viewer near-dupe — **po pilotażu**
- [ ] C5. Skille `organizer-*` współdzielone przez `.claude/skills` i `.agents/skills`; dopasować komendy do realnych nazw skryptów po B1–B14
- [x] C7. Delegacja multi-model: agenci projektu `codex` (`codex exec`, bez `--full-auto`) i `agy` (Antigravity CLI = Gemini; `muxer:gemini` szuka nieobecnej binarki `gemini`), `GEMINI.md` → `AGENTS.md`, `thresholds.yaml: llm` per zadanie (`classify` → `agy_cli`, `relate` → `claude_cli`), tabela koordynatora i biling (Pro, nie Max) w `CLAUDE.md`, permissions `codex exec`/`agy -p`/`claude -p` w `settings.json` (2026-09-17)
- [x] C8. Smoke test delegacji `codex` i `agy` — **oba agenty poprawione po testach**: (a) `codex exec` bez `--ignore-user-config` szedł przez `claude-code-router` (proxy `127.0.0.1:3456`) na `Claude Code API/claude-sonnet-5`, czyli delegacja „za darmo" zjadała limit Anthropic; z flagą CLI raportuje `provider: openai` (`gpt-6-astra`), zadanie read-only wykonane, `git status` bez zmian; (b) `agy -p --sandbox` w headless auto-odrzuca KAŻDE uprawnienie narzędzia (`read_file`, `command` → `jetski: no output produced`), więc Gemini nie przeczyta pliku sam — działa natomiast wklejenie treści do promptu (zweryfikowane na `AGENTS.md`: poprawne streszczenie, `--sandbox` zachowany, zero narzędzi). Ustalenia są w `.claude/agents/{codex,agy}.md` i adapterze `CLAUDE.md`. (2026-09-17) **Uwaga: zalecenie z (a) — `--ignore-user-config` — zostało wycofane w C15;** obserwacja (b) o `agy` nadal obowiązuje i ma teraz czytelny błąd zamiast pustki (C17).
- [x] C9. ~~Decyzja: zostawić przejęcie bazowego `codex` przez `claude-code-router`~~ — **wycofane 2026-09-18, patrz C15.** Interaktywny GPT nadal przez `just codex` i profil `~/.codex/paczka-openai.config.toml` (launcher sprawdza `model_provider = "openai"`). (2026-09-17)
- [x] C15. `claude-code-router` usunięty z bazowego `~/.codex/config.toml` (kopia: `~/.codex/config.toml.pre-ccr-removal-*`). Powody, wszystkie zmierzone: (a) demon CCR nie działał — nic nie nasłuchiwało na `127.0.0.1:3456`, więc czysty `codex` był po prostu zepsuty (`codex doctor`: 1 fail, reachability); (b) podstawiony `model_catalog_json` powodował `warning: Model metadata for gpt-6-astra not found` nawet na profilu `paczka-openai`; (c) obejście flagą `--ignore-user-config` odcina `[hooks.state]`, więc Codex **przestawał uruchamiać** `.codex/hooks.json` — guard `00_SOURCES` milkł (test różnicowy: bez flagi w logu jest `hook: PreToolUse`, z flagą nie ma). Po czyszczeniu `codex doctor` = 19 ok / 0 fail, `provider: openai`, hook działa. Delegacja używa teraz `-c model_provider="openai"` zamiast `--ignore-user-config` (agent `codex`, backend `codex_cli` w `llm_client.py`). Binarka `ccr` została na maszynie, ale nic jej nie używa. (2026-09-18)
- [x] C16. Polityka kosztowa: `thresholds.yaml: llm` kieruje `classify` **i** `relate` na `codex_cli`/`gpt-6-astra`; koordynator (Claude/Opus) planuje i ocenia, ale nie klasyfikuje w sesji. Skill `/organizer-ai-resolve` przestał forkować podagenta `haiku` i jest teraz opakowaniem na `just subject-ai-resolve`. Zasada zapisana w `CLAUDE.md` („Zasada kosztowa") i `AGENTS.md`. (2026-09-18)
- [x] C17. Naprawione dwa błędy backendów w `llm_client.py`, oba wychodzące dopiero przy realnym wywołaniu (testy z mockiem `runner` ich nie łapały): (a) `codex_cli` budował `codex --ignore-user-config exec ...`, a w codex-cli 0.154 ta flaga należy do podkomendy `exec` — backend zwracał `error: unexpected argument` i **nigdy nie działał end-to-end**; (b) `agy_cli` przy `status=SUCCESS` i pustym `response` (model sięgnął po narzędzie, sandbox headless odrzucił) przepuszczał pustkę do parsera JSON, dając mylące „nie znaleziono poprawnego JSON w odpowiedzi: ''” zamiast diagnozy. (2026-09-18)
- [x] C10. Warstwa agent-agnostic: kanoniczne `AGENTS.md`, cienkie adaptery `CLAUDE.md`/`GEMINI.md`, wspólny guard źródeł dla Claude i Codexa, współdzielone skills przez symlinki, launchery `just claude|codex|codex-read|codex-ship`, agent doctor. (2026-09-17)
- [x] C11. Backend AI per host bez edycji YAML: `just claude` ustawia `PACZKA_LLM_RELATE_BACKEND=claude_cli`, `just codex` ustawia `codex_cli`; `LLMConfig.task()` respektuje jawne `PACZKA_LLM_<TASK>_{BACKEND,MODEL}` i nie przenosi modelu między providerami. (2026-09-17)
- [x] C12. Natywne subagenty Codexa: `.codex/config.toml` + 8 ról w `.codex/agents/` (`explorer`, `runner`, `reviewer`, Python, SQL, testy, docs, README), z centralnym modelem i per-rola reasoning/sandbox; kontrakt chroniony testami TOML. (2026-09-17)
- [x] C13. Automatyczny dobór pięciu skilli zgodnie z preferencją użytkownika: Claude `disable-model-invocation: false`, Codex `policy.allow_implicit_invocation: true`; opisane warunki użycia i parametry z kontekstu, bramka akceptacji przed `apply` zachowana. Projektowy `just skills-check` zamiast niezgodnego walidatora ogólnego; 32 testy metadanych, polityk i symlinków. (2026-09-18)
- [x] C14. Decyzja użytkownika: domyślne lokalne commity sprawdzonych zmian narzędzi (kod/config/skrypty/testy/docs/raporty), bez dodatkowego pytania; materiały wyłączone niezależnie od lokalizacji, dotychczasowe bramki apply/verify zachowane. Reguła w głównym `AGENTS.md`, zasadach organizera i skillach; push/PR nadal osobno. (2026-09-17)
- [x] C6. `setup/install.sh` — sprawdzone: apt stawia rmlint/ncdu/tesseract(+pol)/poppler/jq/rclone (rclone dopisany), `just` z just.systems, venv z requirements; `gh` tylko wykrywany (instalacja ręczna wg README) (2026-09-17)

## D. Pilotaż i przedmioty

- [ ] D1. Pilotaż **AKO, sem 3** end-to-end (scan → … → PR); zebrać wnioski do B/C
- [ ] D2. Kolejność dalszych przedmiotów: semestr po semestrze SEM1→SEM7; kolejka i postęp w generowanym `STATUS.md` (nie tutaj)
- [ ] D3. Semestry magisterskie: uzupełnić `subjects.yaml: magisterskie` przed ich przetwarzaniem

## E. Dokumentacja

- [x] E1. README: sekcja „Skrypty” z realnymi nazwami i kolejnością po A3–A6 (2026-09-17)
- [x] E2. `reports/SOURCES_TREE.md` (generowany przez `scan.py`; pierwszy snapshot: 14 paczek, 9 540 katalogów, 48 049 plików, 37,0 GiB) (2026-09-17)
- [ ] E3. `STATUS.md` (generowany: przedmioty × etapy + liczby)
- [x] E4. `reports/HANDOFF.md` + `just handoff`: wspólny checkpoint sesji dla Claude, Codexa i człowieka. (2026-09-17)
- [x] E5. Porządek plików: generowany `SOURCES_TREE.md` w `reports/`, historyczne raporty w `reports/bootstrap/`, mapa raportów i aktualizacja odwołań; kod pipeline'u i dane poza repo bez przenosin. (2026-09-18)
