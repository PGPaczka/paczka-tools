"""orglib — wspólna warstwa danych Paczka Organizer (config + SQLite + hashe).

Importowana przez skrypty etapów uruchamiane jako ``python scripts/<stage>.py``
(katalog ``scripts/`` jest wtedy na ``sys.path``), więc nie wymaga instalacji.
"""
