# Zapisana mutacja: psuje jedno zachowanie i wymaga, by testy to wykryły.
# Uruchom: just mutate-check
name: numery slotów dopełniane do meta.number_padding (kol_03, nie kol_3)
file: scripts/orglib/classify.py
find: '                return str(value).lstrip("0").zfill(padding) if str(value).strip("0") else "0" * padding'
replace: '                return str(value)'
tests:
- tests/test_classify.py
