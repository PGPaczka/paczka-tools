# Zapisana mutacja: psuje jedno zachowanie i wymaga, by testy to wykryły.
# Uruchom: just mutate-check
name: konwerter dostaje ścieżkę absolutną (nazwa nie udaje flagi)
file: scripts/orglib/textextract.py
find: str(Path(os.path.abspath(path)))
replace: str(path)
tests:
- tests/test_special_names.py
