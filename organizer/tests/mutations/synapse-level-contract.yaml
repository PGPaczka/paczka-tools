# Zapisana mutacja: psuje jedno zachowanie i wymaga, by testy to wykryły.
# Uruchom: just mutate-check
# Prototyp synapse buduje filtr poziomów z literału [1,2,3] (`levelList=[1,2,3].map`).
# Notatka z innym poziomem nie wywala niczego u nas — po prostu wypada z filtrów
# w cudzym UI. Dlatego kontrakt musi być pilnowany po naszej stronie.
name: eksport do synapse pilnuje dozwolonych poziomów notatki
file: scripts/orglib/synapse.py
find: '        if self.level not in LEVELS:'
replace: '        if False:'
tests:
- tests/test_synapse.py
